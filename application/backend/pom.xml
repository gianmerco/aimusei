<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>it.prismaprogetti</groupId>
  <artifactId>aimusei-build</artifactId>
  <version>0.0.1-SNAPSHOT</version>
  <packaging>pom</packaging>

  <!-- Inerita BOM e plugin management di Spring Boot (BOM implicito) -->
  <parent>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-parent</artifactId>
    <!-- Scegli una sola versione e usala in tutto il repo -->
    <version>3.5.6</version>
    <relativePath/>
  </parent>

	<modules>
		<module>common</module>
		<module>aiservice</module>
		<module>aimusei</module>
	</modules>

  <properties>
    <java.version>21</java.version>
  </properties>

	<!-- Se usi un repository privato per altre dipendenze, dichiaralo qui -->
	<!--
  <repositories>
    <repository>
      <id>ai-solution-repository</id>
      <url>https://devops.sytes.net/FactoryCollection/AI-Solution/_packaging/ai-solution-repository/maven/v1</url>
      <releases><enabled>true</enabled></releases>
      <snapshots><enabled>false</enabled></snapshots>
    </repository>
  </repositories>
  -->

	<!-- Nessun spring-boot-maven-plugin qui -->
</project>